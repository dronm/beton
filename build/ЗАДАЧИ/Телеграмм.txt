1) Развернуть микросервис на сервере. Настроить отправку СМС, мыла
Как реализровать отправку сообщений телеграмм?
PERFORM pg_notify('Message.tm', NULL);
PERFORM pg_notify('Message.sms', NULL);
PERFORM pg_notify('Message.email', NULL);

Крутится программа слушает базу на события: Message.tm Message.sms(?),Message.email(?)

Добавление нотификация
notifications.add_notification(in_params jsonb)

 * messages {
 * 	ext_obj json
 * 	tm {
 *		структура телеграм
 * 	}
 * 	sms {
 *		структура СМС
 * 	}
 * 	email {
 *
 * 	}
 * }

controls/Chat.js
Контейнер для объектов Notification_View.js
Метод регистрации сообщения
+ Добавление нового чата вручную или автоматом, если такого чата еще нет



Что надо еще сделать:
1) Прием обратных сообщений. messages_in. В параметры фирмы добавить URL для нотификации при постплении сообщения.
Т.е. при входящем сообщении вставка в таблицу - событие - вызов URL по фирме

2) ГУИ формы:
	2.1) Список всех контактов с Телеграм, возможность отправки сообщения.
	2.2) Возможность отправки сообщения из : контрагенты, пользователи
	2.3) Просмотр всех входящих/исходящих в виде таблицы.
	2.4) Просмотр лога
	2.5) Управление параметрами фирмы
	2.4) Отображение чата по пользователю
	
XXX 3) ЛОгирование 
	XXX 3.1) Уровень лога. Задается в фирме
	notifications.log
	date_time
	text
	service: notifications
	tm/sms/email
	level int
	quant (?) int - количество однотипных сообщений
	При вставке, если последнее сообщение по всем параметрам совпадает - заменять
	elkon_log_upsert()
	
	XXX 3.2) Все сообщения во время отправки логировать в лог через функцию
